// Paramètre : DossierDonnees
// ---------------------------------------------------------------------------
// Chemin du dossier contenant les CSV. Il DOIT se terminer par une barre oblique inverse.
// Pourquoi un paramètre ? Si le projet est cloné sur un autre ordinateur, on
// modifie ce seul paramètre au lieu de corriger le chemin dans chaque requête.
//
// Création : Accueil > Transformer les données > Gérer les paramètres > Nouveau
//   Nom           : DossierDonnees
//   Type          : Texte
//   Valeur actuelle : (chemin ci-dessous, adapté à votre poste)

"D:\PROJET PERSO\DATA\BI Dashboard (Power BI )\maven-toys-powerbi-analytics\data\raw\" meta [IsParameterQuery = true, Type = "Text", IsParameterQueryRequired = true]
